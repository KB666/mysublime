git
	https://blog.csdn.net/hanhailong726188/article/details/46738929
	一个版本控制程序
使用方法
	先配置
	git global user.name "名字"
	git global user.email 邮箱
	检查配置信息
	git config --list
	先初始化一个站点文件夹（F:\GIT）
	在该文件夹下的cmd窗口下执行git init初始化git文件夹
将工作目录的文件放到Git仓库
	git add 文件名
	git commit -m “描述你add文件时干了什么”
查看git（暂存区域）状态
	git status
	有三种状态
	modified（已修改）
	staged（已暂存）
	committed（已提交）








git可以分为三个区域
工作区域         	 暂存区域			Git仓库
(文件的存放区域)		(提交前放置的区域)	(提交后文件位于git仓库)
